{
    "content": ":::card\n  # 基本用法\n  - 编码语言：\n    - JavaScript\n    - TypeScript  \n  - 代码风格\n    - 组合式API（Composition API）\n    - 选项式API（Options API）\n  - 简写形式\n    - setup 语法糖\n  - 核心\n    - ref、reactive、computed、watch、生命周期……\n    - 常用：hooks、自定义 ref、路由、pinia、mitt……  \n:::  \n\n\n:::card\n## Vue3 概述\nVue3.0 发布于 2020年9月18日\n  \n- **性能提升**：\n  - 大包大小减少 41%\n  - 初次渲染、更新渲染更快\n  - 内存减少\n- **源码升级**：\n  - 使用 Proxy 代替 defineProperty 实现响应式\n  - 重构 DOM 的实现和 Tree-Shaking\n- **拥抱 TypeScript**\n- **新的特性**\n:::\n\n:::card\n## 创建 Vue3 工程\n\n推荐使用 Vite 创建\nVite 是前端构建工具，官网地址：***https://vitejs.cn***\n  \n### Vite 优势如下：\n- 轻量快速的热加载（HMR），能实现极速的服务启动\n- 对 TypeScript、JSX、CSS 等支持开箱即用\n- 真正的按需编译，不再等待整个应用编译完成\n  \nwebpack 与 vite 构建对比图如下：\n\n### 创建命令：\n```bash\nnpm create vue@latest\n```\n:::\n\n:::card\n## Vue3 核心语法\n  \n### Options API 与 Composition API\n\n- Vue2 的 API 设计是 Options（配置）风格的\n- Vue3 的 API 设计是 Composition（组合）风格的\n  \n#### Options API 的弊端：\nOptions 类型的 API，数据、方法、计算属性等，是分散在：data、methods、computed 中的，若想新增或者修改一个需求，就需要分别修改：data、methods、computed，不便于维护和复用。\n  \n#### Composition API 的优势：\n可以用函数的方式，更加优雅的组织代码，让相关功能的代码更加有序的组织在一起。\n:::\n\n:::card\n## setup\n\nVue3 中一个新的配置项\n\n```html\n<script lang=\"ts\" setup name=\"Person123\">\n```\n\n带有 setup 的 script 标签需要可以直接写组件名，需要额外安装插件 `vite-plugin-vue-setup-extend`\n:::\n\n:::card\n## Ref 创建：基本类型的响应式数据\n  \n- **作用**：定义响应式变量\n- **语法**：`let xxx = ref(初始值)`\n- **返回值**：一个 RefImpl 的实例对象，简称 ref 对象或 ref，ref 对象的 value 属性是响应式的\n  \n> **注意**：  \n1.JS 中操作数据需要：xxx.value，但模板中不需要 .value，直接使用即可  \n2.对于 let name = ref(\"张三\") 来说，name 不是响应式的，name.value 是响应式的\n\n:::\n\n:::card\n## Reactive：只能定义对象类型的响应式数据\n\n### ref 与 reactive 的比较：\n- Reactive 只能定义对象类型的响应式数据\n- ref 可以定义基本类型和对象类型的响应式数据\n- ref 底层使用的是 reactive\n\nReactive 重新分配一个新对象，会失去响应式（可以使用 Object.assign 去整体替换）\n:::\n\n:::card\n## Computed\n\n（计算属性，与 Vue2 用法类似）\n:::\n\n:::card\n## Watch\n\n- 监听数据变化，与 vue2 中的 watch 作用一致。\n\n- Vue3 中的 watch 只能监听以下四种数据：\n  1. ref 定义的数据\n  2. reactive 定义的数据\n  3. 函数返回一个值（getter 函数）\n  4. 一个包含上述内容的数组\n\n### 情况说明：\n\n- **情况二**：监视 ref 定义的 **对象类型** 数据，监视的是对象的地址值，若想监视对象内部属性的变化，需要手动开启深度监视\n- **情况三**：监视 reactive 定义的 **对象类型** 数据，且默认是深度监视，不需要手动关闭\n- **情况四**：监视 ref 或 reactive 定义的 **对象类型** 数据中的某一个属性，注意如下：\n  1. 若该属性值不是 **对象类型**，需要写成函数形式\n  2. 若该属性值是 **对象类型**，可以直接用，也能写成函数用，更推荐写成函数\n\n  结论：监视的要是对象的属性，那么最好写函数式，注意点：若是对象，监视的是地址值，需要关注对象内部，需要手动开启深度监视\n\n- **情况五**：监视多个数据，使用数组\n:::\n\n:::card\n## WatchEffect\n\n```javascript\n// 使用 watchEffect，不用指定需要监视的数据，会自动收集依赖，并且会立即运行回调函数\nwatchEffect(() => {\n  if (temperature.value >= 60 || waterHeight.value >= 80) {\n    console.log('警告，水温过高，请加强防护')\n  }\n})\n```\n:::\n\n:::card\n## Ref\n\n（参考前面的 Ref 创建部分）\n:::\n\n:::card\n## 生命周期\n\n| 时期 | Vue2 | Vue3 |\n|------|------|------|\n| created | 创建前（beforeCreate），创建完毕（created） | setup |\n| mounted | 挂载前（beforeMount），挂载完毕（mounted） | 挂载前（onBeforeMount），挂载完毕（onMounted） |\n| updated | 更新前（beforeUpdate），更新完毕（updated） | 更新前（onBeforeUpdate），更新完毕（onUpdated） |\n| unmounted | 销毁前（beforeDestroy），销毁完毕（destroyed） | 卸载前（onBeforeUnmount），卸载完毕（onUnmounted） |\n\n> 多层组件时，由子到父的顺序渲染，子组件渲染完毕再渲染父组件\n:::\n\n:::card\n## 路由\n\n### 两种路由模式对比：\n\n1. **Hash 模式（默认）**\n   - URL 中带有 #，如：http://localhost:5173/#/about\n   - 使用 URL 的 hash 来模拟路由\n   - 优点：兼容性好，不需要服务器配置\n   - 缺点：URL 不美观，SEO 不友好\n\n2. **History 模式**\n   - URL 没有 #，如：http://localhost:5173/about\n   - 使用 HTML5 History API\n   - 优点：URL 美观，SEO 友好\n   - 缺点：需要服务器配置支持\n\n### 选择路由模式：\n```javascript\nhistory: createWebHistory()       // history 模式\nhistory: createWebHashHistory()   // hash 模式\n```\n:::\n\n:::card\n### 路由传参\n\n1. **query 传参**\n   - /news/detail?id=1&title=\"new1\"\n   - \\{ path: \"/news/detail\", query: { id: 1, title: \"new1\" } \\}\n\n2. **params 传参**\n   - 需要在路由器中的 path 路径里添加占位：/news/detail/\\:id/\\:title\n   - 跳转时写成：/news/detail/new1或：{ name: \"/news/detail\", params: { id: 1, title: \"new1\" } }\n\n> **注意**：  \n1.params 不能传对象数据  \n2.使用非必传数据需在后面加上 ? 号，如：'/news/detail/\\:id/\\:title?'\n:::\n\n:::card\n### 路由的 props 配置\n\n在路由器中，给组件作为 props 数据进行传递\n\n```javascript\n{\n  path: \"detail\",\n  name: \"detail\",\n  component: Detail,\n  // 第一种写法：将路由收到的所有params参数作为props传递给detail组件\n  // props: true,\n  // 第二种写法：可以自己决定将什么作为props传递给detail组件\n  // props: (route) => {\n  //   return route.query;\n  // },\n  // 第三种写法：可以写成对象作为props传递给detail组件\n  // props: { id: 1, title: \"title1\" }\n}\n```\n:::\n\n:::card\n### replace 配置\n\n作用：控制路由跳转时操作浏览器历史记录的模式\n\n浏览器的历史记录有两种方式：push 和 replace：\n- push：追加历史记录（默认）\n- replace：替换当前记录\n\n开启 replace 模式：\n```html\n<RouterLink replace ……>News</RouterLink>\n```\n:::\n\n:::card\n### 编程式导航\n\n在 function 中跳转路由：\n\n```javascript\nimport { useRouter } from 'vue-router'\nconst router = useRouter()\nrouter.push({ path: `/news/detail`, query: { ...n } })\n```\n:::\n\n:::card\n## Pinia 状态管理\n\n### 安装 Pinia\n```bash\nnpm install pinia\n```\n\n### 在 main 文件中：\n```javascript\n// 引入createApp用于创建应用\nimport { createApp } from \"vue\"\n// 创建pinia\nconst pinia = createPinia()\n// 使用pinia\napp.use(pinia)\n```\n\n### 在 store 中创建相应的 store 数据\n\n```javascript\nimport { defineStore } from \"pinia\"\n\n// 选项式\nexport const useTalkStore = defineStore(\"talk\", {\n  state: () => {},\n  actions: {},\n  getters: {}\n})\n\n// 组合式\nexport const useTalkStore = defineStore(\"talk\", () => {\n  return {}\n})\n```\n\n### 组件中使用\n```javascript\nimport { useTalkStore } from \"@/store/count\"\nimport { storeToRefs } from 'pinia'\n\nconst talkStore = useTalkStore()\nconst { talkList } = storeToRefs(talkStore)\n\nfunction getLoveTalk() {\n  talkStore.getLoveTalk()\n}\n```\n\n> 有组合式和选项式两种\n:::\n\n:::card\n## 组件之间的通讯\n\n- **Props 通讯**：只建议父子之间传递\n- **Mitt**：订阅与触发\n- **v-model**\n- **attrs**\n- **$refs 与 $parent**：只用于父传子（$refs）和子传父（$parent）\n- **插槽**：默认插槽、具名插槽、作用域插槽\n\n```html\n<Category>\n  <template v-slot:center>\n    <p>中间插槽</p>\n  </template>\n  <template #top>\n    <!-- 两种写法：v-slot:top 或 #top -->\n    <p>顶部插槽</p>\n  </template>\n  <!-- 作用域插槽 -->\n  <template v-slot:bottom=\"{ list }\">\n    <ul>\n      <li v-for=\"l in list\" :key=\"l\">{{ l }}</li>\n    </ul>\n  </template>\n</Category>\n```\n:::\n\n:::card\n## 其它 API\n\n| API | 说明 |\n|-----|------|\n| shallowRef / shallowReactive | 浅层次修改，只管第一层修改，用于整体修改 |\n| readonly / shallowReadonly | 使数据为只读 |\n| toRaw | 保存响应式数据的原始数据，得到一个普通对象，而不是响应式对象 |\n| customRef | 自定义 ref |\n:::\n\n:::card\n## Vue3 新组件\n\n- Teleport\n- Suspense\n:::"
}